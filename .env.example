# URL del backend desplegado en Railway.
# El servidor de desarrollo de Vite hace proxy de /api/* hacia esta URL.
# Si corres el proyecto localmente apuntando a Railway, déjalo como está.
# Si corres el backend local, cambia a http://localhost:8080
VITE_BACKEND_URL=https://aetheris-production-3f46.up.railway.app
